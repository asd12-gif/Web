@model lab8.Models.CarModel

@{
    ViewData["Title"] = "Xóa Dòng Xe";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-0 shadow-sm" style="border-radius: 15px;">
                <div class="card-body p-5 text-center">
                    <div class="mb-4">
                        <i class="bi bi-exclamation-octagon-fill text-danger" style="font-size: 4rem;"></i>
                    </div>

                    <h3 class="fw-bold text-dark">Xác nhận xóa dòng xe?</h3>
                    <p class="text-muted">
                        Bạn có chắc chắn muốn xóa dòng xe <strong class="text-purple-dark">@Model.Name</strong>?
                        <br />
                        <span class="text-danger small"><i class="bi bi-info-circle"></i> Hành động này có thể ảnh hưởng đến các dữ liệu xe liên quan.</span>
                    </p>

                    <div class="bg-light p-3 rounded-3 mb-4 text-start border-start border-purple-main border-4">
                        <small class="text-uppercase fw-bold text-muted d-block mb-1">Thông tin chi tiết:</small>
                        <div class="d-flex justify-content-between mb-1">
                            <span>Mã dòng xe:</span>
                            <span class="fw-bold">#@Model.Id</span>
                        </div>
                        <div class="d-flex justify-content-between">
                            <span>Tên dòng xe:</span>
                            <span class="fw-bold">@Model.Name</span>
                        </div>
                    </div>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="Id" />

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-danger shadow-sm py-2 fw-bold">
                                <i class="bi bi-trash3 me-2"></i>Xác nhận xóa vĩnh viễn
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary py-2 border-0">
                                Hủy bỏ và quay lại
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>