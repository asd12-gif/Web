@model lab8.Models.Brand
<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-0 shadow-sm" style="border-radius: 15px;">
                <div class="card-body p-5 text-center">
                    <div class="mb-4">
                        <i class="bi bi-exclamation-triangle-fill text-danger" style="font-size: 4rem;"></i>
                    </div>
                    <h3 class="fw-bold text-dark">Xác nhận xóa?</h3>
                    <p class="text-muted">Bạn có chắc chắn muốn xóa hãng xe <strong class="text-purple-dark">@Model.Name</strong> không? Hành động này không thể hoàn tác.</p>

                    <div class="bg-light p-3 rounded-3 mb-4 text-start">
                        <small class="text-uppercase fw-bold text-muted d-block mb-1">Thông tin bản ghi:</small>
                        <span class="d-block"><strong>ID:</strong> #@Model.Id</span>
                        <span class="d-block"><strong>Tên:</strong> @Model.Name</span>
                    </div>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-danger shadow-sm py-2 fw-bold">XÓA VĨNH VIỄN</button>
                            <a asp-action="Index" class="btn btn-outline-secondary py-2 border-0">Quay lại danh sách</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>